apiVersion: v1
kind: ServiceAccount
metadata:
  name: eaf-agent
  namespace: eaf
  annotations:
    # IRSA: EKS injects AWS credentials for this IAM role into the pod automatically.
    # The role is created in workloads/dev/iam.tf (Enterprise-Agent-Framework-Infra repo).
    eks.amazonaws.com/role-arn: arn:aws:iam::718438899462:role/eaf-dev-agent-role
